#!/usr/bin/env sh

echo "🔍 Running pre-commit checks..."

# Run lint-staged (linting and formatting only)
if ! npx lint-staged; then
  echo "❌ Pre-commit checks failed. Please fix the errors above and try again."
  exit 1
fi

echo "✅ Pre-commit checks passed!"
